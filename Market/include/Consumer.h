#ifndef CONSUMER_H
#define CONSUMER_H


class Consumer
{
    public:
        Consumer();
        virtual ~Consumer();

        //Inputs
        //Income (from person)
        //Income history (from person)
        //Prices of products (from market)

        //Output
        //Consumption (from person)
        //Consumption history (optional)
        //

        //Properties:
        //Preferences of products


        //Intermediate outputs

        //



        //std::vector<double> GetConsumedProducts(double dDisposableIncome);

    protected:
/*
        //
        double GetFeedDemand(double dLevel);
        double GetAccomodationDemand(double dLevel);
        double GetClothingDemand(double dLevel);

        //inversas
        double GetLevelForFeedDemand(double dFeedDemand);
        double GetLevelForAccommodationDemand(double dAccommodationDemand);
        double GetLevelForClothingDemand(double dClothingDemand);

        std::vector<double> GetProductsDemandForFeedUnit(double dLevel);
        std::vector<double> GetProductsDemandForAccomodationUnit(double dLevel);
        std::vector<double> GetProductsDemandForClothingUnit(double dLevel);





*/

    private:
};

#endif // CONSUMER_H
